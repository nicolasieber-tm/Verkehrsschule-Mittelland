services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: vsm
      POSTGRES_PASSWORD: vsm
      POSTGRES_DB: vsm
    ports:
      - "5432:5432"
    volumes:
      - vsm_pg_data:/var/lib/postgresql/data

volumes:
  vsm_pg_data:
